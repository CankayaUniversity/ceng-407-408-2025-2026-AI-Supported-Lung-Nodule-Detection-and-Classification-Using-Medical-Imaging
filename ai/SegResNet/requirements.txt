# =============================================================================
# SegResNet: 2.5D Lung Nodule Segmentation
# Python 3.9+ recommended for best compatibility
# =============================================================================

# Core numerical and data handling
numpy>=1.23.0,<2.0
pandas>=1.4.0
PyYAML>=6.0

# Visualization
matplotlib>=3.5.0
opencv-python>=4.6.0

# Medical imaging libraries
pydicom>=2.3.0       # DICOM file reading
SimpleITK>=2.2.0     # Medical image I/O and processing
scikit-image>=0.19.0 # Image processing utilities

# Deep learning framework
torch>=2.0.0,<2.1.0     # PyTorch (install CUDA version if GPU available)
torchvision>=0.15.0     # Vision utilities
monai>=0.9.0,<0.10.0    # MONAI medical imaging library

# Training and logging
tensorboard>=2.10.0
tqdm>=4.64.0

# Optional development tools
jupyter>=1.0.0
ipython>=8.0.0

# =============================================================================
# PYTHON VERSION NOTE:
# - Minimum: Python 3.9
# - Recommended: Python 3.10 or 3.11
# - Avoid: Python 3.12+ (some libraries may not be compatible yet)
#
# TORCH/CUDA COMPATIBILITY:
# - CPU-only: torch>=2.0.0 (already included)
# - GPU (CUDA 11.8): Install torch with: 
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# - GPU (CUDA 12.1): Install torch with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# INSTALLATION:
# pip install -r requirements.txt
# =============================================================================
